<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GP16Editor.MainPage"
             xmlns:viewModels="clr-namespace:GP16Editor.ViewModels"
             xmlns:views="clr-namespace:GP16Editor.Views"
             x:DataType="viewModels:MainViewModel">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Picker Title="Select Input Device"
                    ItemsSource="{Binding InputDevices}"
                    SelectedItem="{Binding SelectedInputDevice}" />

            <Picker Title="Select Output Device"
                    ItemsSource="{Binding OutputDevices}"
                    SelectedItem="{Binding SelectedOutputDevice}" />

            <Button Text="Request Patch"
                    Command="{Binding RequestPatchCommand}" />

            <HorizontalStackLayout Spacing="20">
                <views:EffectSequenceBlockView BindingContext="{Binding BlockAViewModel}" />
                <views:EffectSequenceBlockView BindingContext="{Binding BlockBViewModel}" />
            </HorizontalStackLayout>

            <views:CompressorView IsVisible="{Binding IsCompressorEnabled}" />
            <views:DistortionOverdriveView IsVisible="{Binding IsDistortionOverdriveEnabled}" />
            <views:PickingFilterView IsVisible="{Binding IsPickingFilterEnabled}" />
            <views:StepPhaserView IsVisible="{Binding IsStepPhaserEnabled}" />
            <views:ParametricEQView IsVisible="{Binding IsParametricEQEnabled}" />
            <views:NoiseSuppressorView IsVisible="{Binding IsNoiseSuppressorEnabled}" />
            <views:ShortDelayView IsVisible="{Binding IsShortDelayEnabled}" />
            <views:ChorusView IsVisible="{Binding IsChorusEnabled}" />
            <views:FlangerView IsVisible="{Binding IsFlangerEnabled}" />
            <views:PitchShifterView IsVisible="{Binding IsPitchShifterEnabled}" />
            <views:SpaceDView IsVisible="{Binding IsSpaceDEnabled}" />
            <views:AutoPanpotView IsVisible="{Binding IsAutoPanpotEnabled}" />
            <views:TapDelayView IsVisible="{Binding IsTapDelayEnabled}" />
            <views:ReverbView IsVisible="{Binding IsReverbEnabled}" />
            <views:LineoutFilterView IsVisible="{Binding IsLineoutFilterEnabled}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

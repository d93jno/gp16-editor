<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GP16Editor.ViewModels"
             xmlns:controls="clr-namespace:GP16Editor.Controls"
             x:DataType="vm:CompressorViewModel"
             x:Class="GP16Editor.Views.CompressorView">
    <Border Stroke="LightGray" StrokeThickness="1" Padding="5">
        <VerticalStackLayout>
        <Label Text="Compressor" FontAttributes="Bold" />
        
        <controls:CircularSlider LabelText="Sustain" Minimum="0" Maximum="100" Value="{Binding Sustain}" />
        <controls:CircularSlider LabelText="Attack" Minimum="0" Maximum="100" Value="{Binding Attack}" />
        <controls:CircularSlider LabelText="Tone" Minimum="-50" Maximum="50" Value="{Binding Tone}" />
        <controls:CircularSlider LabelText="Level" Minimum="0" Maximum="100" Value="{Binding Level}" />
    </VerticalStackLayout>
    </Border>
</ContentView>